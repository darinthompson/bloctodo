<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @list.name %>
</p>

<p>
  <b>Items:</b>

<% if @items.length > 0 %>
  <%= will_paginate @items %>
<% else %>
  You have no todo items!  Go get a beer!
<% end %>
</p>

<!-- todo: add "new item" form here; refer to new comment form in bloccit -->

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
